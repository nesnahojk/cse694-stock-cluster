load changedata.txt


newData=zeros(1353,14);


t=0
j=1;
while t<313-24,
x=princomp(logData(:,t+1:t+24));

for i=1:1353,
    newLogData(i,j)=logData(i,t+1:t+24)*x(:,1);
    newLogData(i,j+1)=logData(i,t+1:t+24)*x(:,2);
endfor

t=t+24;
j=j+2;
endwhile
